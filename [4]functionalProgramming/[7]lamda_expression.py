# Lamda Expression
# --> Lamda expression in python are one time anonymous 
# functions you don't need more than once.

numbers = [4, 5, 7, 9]

result = list(map(lambda item: item * 2, numbers))
print(result)  